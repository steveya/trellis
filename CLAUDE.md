# Deprecated

`CLAUDE.md` is no longer the authoritative architecture reference for this
repository.

Use these files instead:

- `ARCHITECTURE.md` for the current high-level codebase architecture
- `AGENTS.md` for workflow, ownership, and implementation rules
- `LIMITATIONS.md` for the live support contract and known gaps
- `docs/quant/pricing_stack.rst` for the deterministic pricing stack
- `docs/developer/overview.rst` for the governed platform/runtime view

This file is kept only as a compatibility pointer so older tooling or habits do
not silently rely on stale architecture notes.
