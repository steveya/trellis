# Known Limitations

This file is a repo-grounded snapshot of current limitations, not a wishlist.

Open entries below were manually revalidated on 2026-04-04 from the checked-in
code and docs, with emphasis on the mathematical, numerical, and computational
gaps that block Trellis from being desk-credible for calibration, exotic-book
pricing/risk, and counterparty-style workflows.

Legacy entries that were present in older versions of this file but were not
rechecked in this pass are preserved under `Needs Revalidation` so their IDs do
not disappear from surrounding docs. They should not be treated as current
ground truth until revalidated.

## Resolved

| # | Limitation | Resolution |
|---|-----------|-----------|
| L1 | ~~Trinomial trees not implemented~~ | `build_rate_lattice()` and `build_generic_lattice()` now support `branching=3`. HW trinomial with mean-reversion probabilities. |
| L4 | ~~Old CN/implicit FD still have coefficient bug~~ | `__init__.py` now imports from `theta_method_1d`. Old names are backward-compat wrappers. Capabilities updated. |
| L9 | ~~YTM not computed~~ | `trellis.engine.pricer.price_instrument()` now solves a coupon-frequency nominal yield-to-maturity from the reported dirty price and projects it through the direct/session pricing surfaces. |
| L15 | ~~Critic/validator failures silently caught~~ | All `except Exception: pass` blocks in `_validate_build()` now log warnings via `logging.warning()`. |
| L16 | ~~Experience recording silently fails~~ | `_record_resolved_failures()` now validates LLM output, logs errors at each stage. Knowledge system capture logged separately. |
| L10 | ~~Accrued interest simplified~~ | Bond accrued interest now uses the explicit coupon schedule plus the selected day-count convention instead of a flat period approximation. |
| L5 | ~~Hull-White helper routes still default mean reversion to `0.1`~~ | Callable-bond, Bermudan-swaption, and ZCB-option helpers now resolve Hull-White parameters from explicit inputs or `MarketState.model_parameters` / `model_parameter_sets`, with legacy heuristics only as fallback. |
| L2 | ~~`YieldCurve.bump()` only hits exact tenor matches~~ | The shared curve-shock substrate now supports off-grid bucket nodes, and `KeyRateDurations` consumes the same bucket grid so interpolation-aware KRD requests no longer collapse to exact-knot-only sensitivities. |
| L39 | ~~Build-loop rejected legitimate signed-PV payoffs~~ | `check_non_negativity` in `trellis/agent/invariants.py` now short-circuits via `_payoff_is_signed_linear` for single-name CDS (and any future signed linear products declared through the same trait helper), so protection-buyer CDS clean PV no longer fails the invariant suite. Family-specific CDS invariants (`check_cds_spread_quote_normalization`, `check_cds_credit_curve_sensitivity`) remain in force. (QUA-851.) |
| L40 | ~~Build-loop enforced vol-monotonicity on digital options~~ | `check_vol_monotonicity` in `trellis/agent/invariants.py` now short-circuits via `_payoff_has_nonmonotonic_vol` for cash-or-nothing / asset-or-nothing digital payoffs (classified by class name, spec name, `cash_payoff` attribute, or `payoff_style` marker), so digital options no longer fail the invariant suite for their legitimately non-monotonic price-vs-vol curves. Pricing-exception detection is preserved; only the monotonicity assertion is skipped. (QUA-879.) |

## Revalidated Open Limitations

### Numerical Methods And Runtime Risk

| # | Limitation | Impact | Files |
|---|-----------|--------|-------|
| L3 | **Scalar runtime vega is only partially surface-native** — `FlatVol` now uses autodiff, bounded portfolio-AAD option risk can differentiate `GridVolSurface` node values for supported European vanilla books, `GridVolSurface` scalar runtime vega still uses an explicit parallel bucket bump, and unsupported surfaces now disclose representative-flat-vol fallback metadata through the shared derivative-method taxonomy instead of silently collapsing; but there is still no true scalar autodiff path for realistic non-flat surfaces outside the declared node-value support contracts | Runtime vega is now auditable, and bounded grid-node book AAD exists for supported vanilla option books, but scalar smile/surface vega outside the declared support contracts remains approximate rather than a true surface-native Greek | `trellis/analytics/measures.py`, `trellis/analytics/derivative_methods.py`, `trellis/models/vol_surface.py`, `trellis/analytics/portfolio_aad.py`, `tests/test_verification/test_portfolio_aad_grid_vol.py` |
| L11 | **Barrier monitoring is discrete fixed-step Monte Carlo** — continuous barriers are approximated by checking a finite path grid | Barrier prices and Greeks require many steps to converge and are not desk-grade for tight barrier risk | `trellis/instruments/barrier_option.py`, `trellis/models/double_barrier_option.py` |
| L12 | **Runtime risk is broader and provenance-aware, but still bump-heavy outside the declared AD lanes** — public `YieldCurve` DV01/duration/convexity and exact-node KRDs can now resolve through `autodiff_public_curve`, and `delta`/`gamma`/`theta` now exist as runtime measures with normalized derivative-method metadata (`resolved_derivative_method`, `derivative_method_category`, `derivative_method_support`, fallback reason, and parameterization/bump fields); but spot risk, theta, off-grid KRDs, rebuild-based rates risk, and most scenario/explain workflows still depend on finite differences or structured bumps | The runtime now states which derivative method it used instead of silently mixing paths, but the overall risk surface is still too bump-oriented for broad exotic-book daily risk or explain use | `trellis/analytics/measures.py`, `trellis/analytics/derivative_methods.py`, `trellis/models/vol_surface.py`, `docs/user_guide/pricing.rst` |
| L25 | **Early-exercise Monte Carlo upper bounds are diagnostic, not production dual bounds** — the current upper bound is a perfect-information pathwise maximum, not a full Andersen-Broadie-style nested dual construction | American/Bermudan MC error control is not strong enough for institutional exercise risk | `trellis/models/monte_carlo/primal_dual.py` |

### Calibration And Model Realism

| # | Limitation | Impact | Files |
|---|-----------|--------|-------|
| L6 | **Supported Hull-White calibration is still constant-parameter only** — the checked workflow fits one `(mean_reversion, sigma)` pair across the strip rather than a time-dependent sigma term structure or cap-surface fit | Tree-based rates pricing now supports a reusable calibrated parameter set, but not a full term-structure calibration workflow | `trellis/models/calibration/rates.py` |
| L17 | **Dupire local vol now flags unstable regions and records continuous-yield carry inputs, but still repairs them with implied-vol fallback rather than a regularized surface fit** | Review tools can now inspect explicit diagnostics, carry metadata, and warnings, but the checked path is still a hardening layer rather than a production arbitrage-repair or discrete-dividend equity-surface workflow | `trellis/models/calibration/local_vol.py` |
| L26 | **Curve bootstrap is still not fully desk-complete even though the checked solve now carries explicit derivative provenance** — Trellis now supports a first dated, dependency-aware multi-curve bootstrap lane and the supported bootstrap solve exposes `autodiff_vector_jacobian` provenance, but the broader curve plant still uses a bounded subset of schedule and futures conventions and has no smoothing, regularized family selection, or industrial adjoint plant | Curve construction is materially stronger, auditable, and derivative-aware, but it is still too narrow for production multi-curve calibration or stable portfolio-scale adjoint sensitivities | `trellis/curves/bootstrap.py`, `trellis/models/calibration/solve_request.py`, `trellis/data/resolver.py`, `docs/mathematical/calibration.rst` |
| L27 | **Rates-vol authority is now reusable but still bounded** — Trellis now supports bounded caplet stripping into a reusable caplet-vol surface and a tenor-aware swaption cube materialized back onto `MarketState`, but the checked path still assumes a one-step caplet-ladder strip, Black-lognormal quotes, and a rectangular absolute-strike cube with no bid/ask governance, normal/shifted-vol support, or arbitrage repair | Downstream cap/floor and swaption pricing can now consume a first calibrated rates-vol market object, but the workflow is still materially narrower than a production caplet/swaption surface plant | `trellis/models/calibration/rates_vol_surface.py`, `trellis/models/rate_style_swaption.py`, `docs/mathematical/calibration.rst` |
| L28 | **SABR calibration is now cube-slice aware but still local to each slice** — Trellis can now fit one SABR smile per expiry-tenor swaption-cube slice and compare those fitted slices back to the reconstructed market cube, but the checked path still has no global cube smoothing, cross-slice regularization, arbitrage repair, or normal/displaced/shifted SABR workflow | Trellis now has a first staged rates-vol model-compression lane, but it is still not a full desk SABR cube plant or broader rates-model selection framework | `trellis/models/calibration/rates_vol_surface.py`, `trellis/models/calibration/sabr_fit.py`, `docs/mathematical/calibration.rst` |
| L29 | **Advanced stochastic-vol integration is still partial** — supported Heston smile calibration with a bounded problem-IR adapter, repaired-surface-driven full-surface Heston compression, explicit `finite_difference_vector_jacobian` solve provenance, runtime binding, checked Heston FFT/COS transform helper binding, checked European Heston Euler/QE Monte Carlo helper binding, Heston ADI PDE binding/diagnostic scaffolding, typed Heston Gauss-Laguerre quadrature blocker contracts, named Bates affine jump-stochastic-vol blocker contracts, SLV/LSV leverage-function blocker contracts, and typed path-dependent Heston control blocker contracts now exist, but the checked path still fits one global Heston parameter pack across the surface, the Heston ADI stencil is diagnostic/numerical scaffolding rather than an unqualified standalone release benchmark, has no checked Heston Gauss-Laguerre transform kernel, no Bates characteristic-function or simulation kernel, no checked SLV/LSV leverage calibration or solver, no Heston path-state/event-monitor/payoff-summary/early-exercise control solver, and broader stochastic-vol reuse remains incomplete | Trellis can now record the Heston smile surface-to-parameter bridge as a calibration problem, compress a bounded repaired equity-vol authority into one reusable Heston parameter set with auditable derivative-method provenance, reuse that pack for checked FFT/COS transform pricing plus bounded European Heston Euler/QE Monte Carlo pricing, expose Heston ADI binding diagnostics for agent-generated PDE work, classify T114-style Heston Gauss-Laguerre tasks by required characteristic-function quadrature inputs and validation blockers, classify Bates tasks by the required Heston-plus-lognormal-jump contract, classify SLV/LSV tasks by required leverage-function inputs/provenance/solver contracts, and classify E27-style American Asian barrier Heston tasks by required path-state, event-monitor, payoff-summary, control-policy, and stochastic-vol coupling blockers, but not a full time-dependent stochastic-vol surface plant, stochastic-local-vol bridge, executable Gauss-Laguerre Heston quadrature route, Bates/SLV/LSV pricing kernels, or path-dependent early-exercise stochastic-vol solvers | `trellis/agent/computational_problem_ir.py`, `trellis/models/calibration/heston_fit.py`, `trellis/models/processes/heston.py`, `trellis/models/transforms/heston.py`, `trellis/models/monte_carlo/stochastic_vol.py`, `trellis/models/monte_carlo/schemes.py`, `trellis/models/pde/heston_adi.py`, `trellis/models/processes/sabr.py`, `docs/mathematical/calibration.rst`, `docs/mathematical/processes.rst`, `docs/mathematical/transforms.rst`, `docs/mathematical/monte_carlo.rst` |
| L45 | **Single-name credit calibration is now schedule-aware and quote-style aware, but still not a production CDS curve plant** — spread, hazard, and standard-coupon-plus-upfront quotes now normalize through the analytical CDS pricer with quote-level CDS schedule metadata, calendar/business-day adjustment, roll/stub, payment-lag, recovery, repricing, survival, and forward-hazard governance diagnostics, but the checked path is still a bounded single-name workflow with no bid/ask governance, index-credit conventions, curve smoothing/regularization, structural/hybrid credit model support, or heterogeneous portfolio calibration | Trellis can now honestly calibrate bounded single-name CDS strips through the pricing engine and materialize the fitted `CreditCurve` for downstream CDS consumers, but it is still not a full production credit curve plant or broader credit calibration stack | `trellis/models/calibration/credit.py`, `trellis/models/credit_default_swap.py`, `docs/mathematical/calibration.rst` |
| L46 | **Equity-vol surface authority is now quote-governed and repaired-surface aware, but still bounded to local outlier cleaning plus per-expiry raw-SVI and discrete calendar repair** — Trellis can now preserve raw-versus-cleaned quote provenance, flag node-level adjustments, fit a multi-expiry repaired equity-vol authority, derive local vol from that repaired surface, and compress that repaired surface into one Heston parameter set for staged comparison, but the checked path is still not a full production bid/ask governance, SSVI, stochastic-local-vol, or SPX/VIX-style volatility program | Trellis now has a real market-object-first equity-vol slice with explicit model-compression staging, but it is still not a full industrial equity-vol plant | `trellis/models/calibration/equity_vol_surface.py`, `trellis/models/calibration/local_vol.py`, `trellis/models/calibration/heston_fit.py`, `docs/mathematical/calibration.rst` |
| L47 | **Basket-credit correlation calibration is homogeneous and exact-node only** — Trellis now fits tranche-implied correlations for homogeneous representative-curve basket fixtures, consumes a materialized single-name `CreditCurve`, records quote residual/root/monotonicity/smoothness/tranche-bound diagnostics, and materializes a `correlation_surface` for downstream tranche consumers, but it does not yet support heterogeneous name-level curves, index-credit conventions, bid/ask tranche markets, interpolated base-correlation bootstraps, global smoothing, or tranche-surface arbitrage repair | Basket-tranche helpers can consume a governed calibrated correlation object instead of relying only on route-local constants, but the result is not a production base-correlation plant or heterogeneous portfolio calibration workflow | `trellis/models/calibration/basket_credit.py`, `trellis/models/credit_basket_copula.py`, `trellis/models/calibration/materialization.py`, `docs/mathematical/calibration.rst`, `docs/mathematical/copulas.rst` |
| L48 | **Hybrid calibration and graph-backed AD are still bounded single-name quanto bridges plus path-summary, early-exercise, and lane-local multi-product fixtures** — Trellis can fit one scalar `quanto_correlation` from quanto option price quotes using already-bound domestic/foreign curves, spots, and volatility inputs, records a dependency DAG plus solver/repricing diagnostics, materializes the result as a `model_parameter_set`, and can now attach a typed bounded quanto `HybridFactorGraph`, compute VJP-backed scalar-coordinate sensitivities and bounded directional HVPs for supported graph-owned underlier spot, FX spot, domestic/foreign curve zero-rate nodes, flat/grid vol nodes, and scalar correlation, compute bounded terminal-quanto direct correlation-matrix-coordinate VJP/HVP away from the PSD boundary, classify ContractIR terminal-quanto semantic admissions for the scalar and matrix VJP/HVP lanes, classify path-dependent/event/early-exercise/dynamic hybrid shapes with typed `HybridADStatePolicy` metadata, return a typed fail-closed `DynamicContractIR` dynamic-state runtime result through `trellis.analytics.fail_closed_dynamic_state_derivative(...)` with a discovery-only `dynamic_state_policy` chart and selected-factor diagnostics, execute one bounded arithmetic-average flat-vol path-summary VJP lane with `hybrid_path_summary_vjp` metadata, return discovery-only grid-vol path-summary policy graphs with `unsupported_grid_vol_interpolation` dependencies instead of executing node VJP, execute one bounded vanilla American/Bermudan flat-vol early-exercise VJP lane with `hybrid_early_exercise_vjp` metadata, return discovery-only grid-vol early-exercise policy graphs with planned hard-exercise-projection control metadata, validate correlation-matrix chart-policy payloads and define discovery-only correlation-surface chart-policy payloads for fail-closed diagnostics, and compose already-executed supported graph-owned Hybrid AD lane results through `trellis.analytics.aggregate_hybrid_ad_lane_results(...)` with quantity-scaled sparse-risk aggregation and structured unsupported-lane diagnostics; but it does not yet support a general rates/equity/FX calibration plant, separate FX-vol surface calibration, multi-underlier quanto baskets, cross-slice smoothing, hybrid arbitrage repair, joint state-model calibration, executable correlation-surface AD, matrix projection/repair or PSD-boundary derivatives, executable grid-vol/event-monitor path-state derivatives, dynamic hybrid state derivatives, grid-vol or boundary-kink early-exercise derivatives, path-summary/early-exercise HVP/JVP, or broad product-family graph-owned hybrid derivatives | Downstream single-underlier quanto pricing can consume a governed calibrated correlation descriptor, auditable bounded scalar-coordinate and matrix-coordinate graph-backed VJP/HVP derivative lanes, semantic admission payloads for supported ContractIR terminal-quanto requests, one bounded arithmetic-average flat-vol path-summary VJP lane, one bounded vanilla flat-vol early-exercise VJP lane, typed state/event fail-closed metadata and a graph-owned dynamic-state evidence packet for unsupported or planned hybrid shapes, checked fail-closed grid-vol path-summary and early-exercise policy diagnostics, checked fail-closed matrix and surface-chart diagnostics, and a bounded multi-product composition surface for already-executed graph-owned lanes, but the broader cross-asset calibration and hybrid-AD roadmap remains explicitly open | `trellis/models/calibration/quanto.py`, `trellis/models/calibration/dependency_graph.py`, `trellis/models/resolution/quanto.py`, `trellis/analytics/hybrid_factors.py`, `trellis/analytics/hybrid_ad.py`, `trellis/analytics/hybrid_ad_admission.py`, `trellis/analytics/hybrid_ad_multi_product.py`, `tests/test_analytics/test_hybrid_dynamic_state.py`, `tests/test_analytics/test_hybrid_path_summary.py`, `tests/test_analytics/test_hybrid_early_exercise.py`, `tests/test_analytics/test_hybrid_ad_multi_product.py`, `tests/test_verification/test_hybrid_ad_multi_product.py`, `docs/mathematical/calibration.rst`, `docs/design_quanto_runtime_contract.md`, `docs/quant/differentiable_pricing.rst`, `docs/quant/ad_aad_methodology.rst`, `docs/user_guide/session.rst`, `docs/developer/overview.rst` |

### Governance And Product Claims

| # | Limitation | Impact | Files |
|---|-----------|--------|-------|
| L49 | **Agent-cycle and institutional approval evidence are governed internal review surfaces, not external model certification** — Trellis now exposes a stable `agent_cycle` result surface for quant/critic/arbiter/model-validator evidence, model-promotion eligibility, and benchmark trigger rates, and `policy_bundle.production.institutional` can require approval, model-review, snapshot, run-artifact, and audit-bundle evidence before production execution, but these surfaces only certify recorded internal governance evidence for the run or model version | Product and desk review surfaces can show why a cycle passed, failed, was unavailable, or lacked required institutional approval artifacts, but they must not be read as external model approval, regulatory sign-off, xVA/FpML coverage, or correctness beyond the recorded validation scope | `trellis/agent/cycle_surface.py`, `trellis/agent/task_runtime.py`, `trellis/platform/models.py`, `trellis/platform/policies.py`, `trellis/platform/services/pricing_service.py`, `docs/developer/audit_and_observability.rst`, `docs/developer/hosting_and_configuration.rst`, `docs/user_guide/pricing.rst` |

### Autograd And Sensitivities

| # | Limitation | Impact | Files |
|---|-----------|--------|-------|
| L30 | **The AD layer has checked VJP/HVP operators and bounded reverse-mode lanes, but no industrial portfolio AAD or universal hybrid AD yet** — `trellis.core.differentiable.get_backend_capabilities()` still declares the active `autograd` backend and its supported `grad=True`, `jacobian=True`, `hessian=True`, `vjp=True`, and `hessian_vector_product=True` operators through an executable-truth `support_matrix` / `operator_support(...)` payload. `hessian_vector_product` is limited to scalar-objective / smooth-interior functions. `jvp=False` remains intentional because stock `autograd.make_jvp` lacks forward-mode coverage for pricing primitives such as `norm.cdf`, and fail-closed hybrid JVP payloads now resolve to `unsupported_hybrid_jvp` with `requested_backend_operator="jvp"` plus backend support metadata instead of advertising an executed JVP backend hook. `portfolio_aad=False` still marks the backend capability contract even though `trellis.book.portfolio_aad_curve_risk(...)` differentiates supported bond books over a shared curve, `trellis.book.portfolio_aad_equity_option_vol_risk(...)` differentiates bounded European call/put books over one shared `FlatVol` or `GridVolSurface` node grid plus bounded American/Bermudan vanilla call/put books over shared `FlatVol` under a hard exercise-projection smooth-interior policy, `trellis.book.portfolio_aad_arithmetic_asian_vol_risk(...)` differentiates bounded arithmetic-average European option books over shared `FlatVol` under a lognormal moment-matching smooth path-summary policy, `trellis.book.portfolio_aad_quanto_correlation_risk(...)` differentiates bounded single-name quanto option books over one scalar underlier/FX correlation, `trellis.book.portfolio_aad_supported_book_risk(...)` aggregates mixed books composed from the explicit supported lanes, `trellis.analytics.differentiate_quanto_scalar_correlation(...)` computes one graph-backed `hybrid_scalar_vjp` sensitivity to a bounded quanto scalar correlation coordinate, `trellis.analytics.differentiate_quanto_scalar_inputs(...)` computes a graph-backed `hybrid_scalar_vector_vjp` sparse vector and `hybrid_scalar_vector_hvp` directional second derivative over supported bounded quanto scalar coordinates carried by `HybridFactorGraph`, `trellis.analytics.differentiate_quanto_correlation_matrix(...)` computes bounded terminal-quanto `hybrid_matrix_vector_vjp` and `hybrid_matrix_vector_hvp` direct off-diagonal matrix-coordinate risk for well-conditioned checked correlation matrices, `trellis.analytics.differentiate_arithmetic_asian_path_summary(...)` computes one bounded arithmetic-average flat-vol `hybrid_path_summary_vjp` lane, `trellis.analytics.differentiate_vanilla_early_exercise(...)` computes one bounded vanilla American/Bermudan flat-vol `hybrid_early_exercise_vjp` lane under a hard exercise-projection smooth-interior policy, `trellis.analytics.admit_hybrid_ad_lane(...)` emits typed `HybridADStatePolicy` metadata for path summaries, discontinuous events, early-exercise controls, and DynamicContractIR state/control requests, `trellis.analytics.fail_closed_dynamic_state_derivative(...)` returns a typed unsupported dynamic-state runtime result with a discovery-only graph-owned policy coordinate, `trellis.analytics.fail_closed_correlation_structure_derivative(...)` validates correlation matrix chart-policy payloads, defines discovery-only correlation-surface chart-policy payloads, and reports deterministic fail-closed matrix and surface-chart diagnostics outside that executable lane, and `trellis.analytics.aggregate_hybrid_ad_lane_results(...)` composes already-computed lane-local `HybridDerivativeResult` values into a quantity-scaled sparse VJP aggregate while preserving unsupported-lane diagnostics. These lanes emit explicit `portfolio_aad_vjp` / `hybrid_scalar_vjp` / `hybrid_scalar_vector_vjp` / `hybrid_scalar_vector_hvp` / `hybrid_matrix_vector_vjp` / `hybrid_matrix_vector_hvp` / `hybrid_path_summary_vjp` / `hybrid_early_exercise_vjp` / `unsupported_hybrid_jvp` / `unsupported_hybrid_structure` / `vjp` / `hessian_vector_product` metadata, canonical factor coordinates, sparse risk vectors, multi-product aggregate risk vectors, graph or bucket aggregation payloads, selected-factor helpers, per-lane support attempts, semantic admission and state-policy diagnostics, dynamic-state/early-exercise/path-summary/hybrid policy diagnostics, and unsupported-position, unsupported-dependency, or unsupported-lane exclusions | Sensitivity throughput is now easier to govern and extend because the bounded lanes have stable factor identity, typed request/result contracts, product-family adapters, semantic admission metadata, mixed supported-book dispatch, bounded hybrid factor graph provenance, local benchmark-shape/latency evidence, finite-difference verification, checked fail-closed state/event/dynamic-state and matrix/surface-chart diagnostics, and bounded multi-product lane aggregation, but Trellis still will not scale to large mixed books, calibration loops, scenario grids, broad hybrid factor graphs, grid-vol early-exercise/path-dependent options, discontinuous event monitors, broad path-dependent/dynamic state or non-vanilla/grid-vol/boundary-kink early-exercise hybrid state, correlation-surface AD, matrix projection/repair or PSD-boundary AD, hybrid JVP, path-summary/early-exercise HVP, or broad product-family AAD until more adapters and larger-scale validation land | `trellis/core/differentiable.py`, `trellis/book.py`, `trellis/analytics/risk_factors.py`, `trellis/analytics/portfolio_aad.py`, `trellis/analytics/portfolio_aad_admission.py`, `trellis/analytics/hybrid_factors.py`, `trellis/analytics/hybrid_ad.py`, `trellis/analytics/hybrid_ad_multi_product.py`, `trellis/analytics/derivative_methods.py`, `trellis/session.py`, `tests/test_analytics/test_portfolio_aad.py`, `tests/test_analytics/test_hybrid_factors.py`, `tests/test_analytics/test_hybrid_dynamic_state.py`, `tests/test_analytics/test_hybrid_ad_multi_product.py`, `tests/test_models/test_quanto_hybrid_factor_graph.py`, `tests/test_verification/test_hybrid_ad_quanto.py`, `tests/test_verification/test_hybrid_ad_matrix.py`, `tests/test_verification/test_hybrid_ad_path_summary.py`, `tests/test_verification/test_hybrid_ad_early_exercise.py`, `tests/test_verification/test_hybrid_ad_multi_product.py`, `tests/test_verification/test_portfolio_aad_option_vol.py`, `tests/test_verification/test_portfolio_aad_grid_vol.py`, `tests/test_verification/test_portfolio_aad_path_dependent.py`, `tests/test_verification/test_portfolio_aad_hybrid.py`, `docs/quant/differentiable_pricing.rst`, `docs/quant/ad_aad_methodology.rst`, `docs/user_guide/session.rst`, `docs/developer/overview.rst` |
| L31 | **Large parts of the numerical stack are still intentionally forward-only** — the public `PricingValue` payoff contract and the public curve/surface abstractions now preserve traced scalars for supported smooth node-value workflows, runtime risk now records normalized derivative-method category/support metadata on scalar and structured outputs, and deterministic Monte Carlo can now keep smooth terminal/snapshot state contracts on the traced path; but streaming reduced-state MC accumulation, discontinuous payoffs, Numba kernels, unsupported surface/risk contracts, and many custom schemes still sit outside the traced region. Barrier-monitor and barrier/exercise event replay pricing now exposes explicit discontinuous derivative metadata (`fail_closed`, `unsupported_discontinuous_pathwise`, `finite_difference_bump_reprice`, `derivative_method_category`, `derivative_method_support`) instead of silently implying pathwise AD support | Trellis can now differentiate through the public payoff boundary, through public yield/credit/grid-vol market objects on supported node-value workflows, through explicit-shock smooth state-aware Monte Carlo contracts, and it can now state when it fell back to bumps or when a discontinuous MC Greek must fail closed; but it still cannot rely on one consistent differentiable path across pricing and risk | `trellis/core/payoff.py`, `trellis/curves/yield_curve.py`, `trellis/curves/credit_curve.py`, `trellis/models/vol_surface.py`, `trellis/analytics/measures.py`, `trellis/analytics/derivative_methods.py`, `trellis/models/monte_carlo/engine.py`, `docs/quant/differentiable_pricing.rst` |
| L32 | **Differentiable Monte Carlo is still explicit-shock and smooth-contract only** — terminal-only and smooth event-replay state-aware payoffs can now stay on the traced path, and barrier-monitor plus barrier/exercise event replay requests now carry a checked fail-closed discontinuous derivative policy through the shared derivative-method taxonomy; but deterministic pathwise gradients still require explicit shocks, supported differentiable schemes remain limited, and barrier/exercise-style event logic or true streaming reduced-state accumulation still fall back or fail closed | Autograd-based MC Greeks now cover a more reusable runtime subset and can disclose discontinuous barrier/event policy metadata, but they are still too constrained for broad production risk, especially for discontinuous event logic or memory-scaled state streaming | `trellis/models/monte_carlo/engine.py`, `trellis/models/monte_carlo/event_aware.py`, `trellis/models/monte_carlo/event_state.py`, `trellis/models/monte_carlo/path_state.py`, `trellis/analytics/derivative_methods.py` |

### Portfolio, Scenario, And Counterparty Risk

| # | Limitation | Impact | Files |
|---|-----------|--------|-------|
| L41 | **Route-free ContractIR cutover is still bounded by decomposition coverage and admitted arithmetic-Asian scope** — admitted requests that decompose into a checked `ContractIR` shape now select exact helpers route-free on the fresh-build path, and arithmetic Asians now have bounded structural analytical call/put support plus the earlier expiry-aligned Monte Carlo call lane; but structurally unsupported, under-specified, or broader arithmetic-Asian variants still fall back to the compatibility route path, so this is not yet universal route retirement | Trellis can now claim route-free fresh-build authority for the migrated bounded cohort including the admitted arithmetic-Asian proving slice, but it is still incorrect to claim generic decomposition closure or broad arithmetic-Asian structural support outside the checked European schedule-based equity-diffusion surface | `trellis/agent/contract_ir_solver_compiler.py`, `trellis/agent/semantic_contract_compiler.py`, `trellis/agent/platform_requests.py`, `trellis/models/asian_option.py`, `docs/quant/contract_ir.rst`, `docs/developer/contract_ir_solver_compiler.rst` |
| L42 | **Quoted-observable closure is bounded to terminal linear quote spreads** — `CurveQuote` / `SurfaceQuote` nodes, bounded decomposition, route-free admission, and checked executable helper bindings now exist for terminal linear curve-spread and surface-spread / vol-skew payoffs, but options on quote spreads, path-dependent quote products, quote-linked coupon notes, and broad market-coordinate / shock-model integration remain out of scope | Trellis can now claim route-free exact backend authority for the first quoted-observable executable cohort, but it is still not support-contract-correct to claim generic quoted-spread pricing, quote-option support, dynamic quote-linked note support, or broad market-coordinate overlay coverage | `trellis/agent/contract_ir.py`, `trellis/agent/knowledge/decompose.py`, `trellis/agent/quoted_observable_admission.py`, `trellis/models/quoted_observable.py`, `docs/quant/contract_ir.rst` |
| L43 | **Static-leg route-free authority is still bounded to the admitted cohort** — route-free decomposition, checked lowering admission, public execution-backed payoff plumbing, and execution-IR repricing now exist for vanilla fixed-float IRS, SOFR/FF-style basis swaps, fixed coupon bonds, scheduled `period_rate_option_strip` contracts, and the single-index `range_accrual` conditional-coupon slice. The admitted cap/floor compatibility wrappers now delegate through the execution-backed runtime, the checked range-accrual static route now lowers to a `conditional_accrual_leg` execution obligation, and the fixed-float IRS cohort can also bridge from the same execution artifact into the checked swap future-value substrate. Richer leg structures outside the admitted constant-notional coupon, basis, scheduled-strip, and single-index conditional-accrual families remain unsupported, including interrupted range accruals, barrier-style accrual state, CMS-spread observables, and multi-index predicates | Trellis can now honestly claim execution-backed route-free repricing for the static-leg proving cohort, execution-IR repricing for checked single-index range accruals, and execution-backed future-value bridging for the bounded vanilla fixed-float IRS slice, but it is still not support-contract-correct to claim generic leg-product coverage, irregular leg-structure coverage, interrupted/CMS-spread range-accrual pricing, generic static-leg exposure simulation, or broad dynamic-wrapper execution | `trellis/core/payoff.py`, `trellis/agent/static_leg_contract.py`, `trellis/agent/static_leg_admission.py`, `trellis/agent/semantic_observables.py`, `trellis/models/range_accrual.py`, `trellis/execution/compiler.py`, `trellis/execution/runtime.py`, `trellis/execution/visitors/`, `trellis/instruments/_agent/agentcap.py`, `trellis/instruments/_agent/agentfloor.py`, `trellis/models/rate_basis_swap.py`, `trellis/models/monte_carlo/simulation_substrate.py`, `docs/quant/static_leg_contract_ir.rst`, `docs/quant/contract_algebra.rst`, `docs/user_guide/pricing.rst` |
| L44 | **Dynamic event/state/control closure is now only partially executable** — `DynamicContractIR` now carries explicit inventory and control-magnitude semantics, bounded decompositions exist for autocallable/TARN, callable-bond/swing, callable range-accrual, and GMWB-style proving fixtures, `compile_dynamic_lane_admission(...)` emits admitted automatic, discrete, and continuous lane contracts with benchmark plans, and overlay-bearing GMWB requests now have an explicit `InsuranceOverlayContractIR` representation instead of only blocker prose. The bounded callable-bond discrete-control cohort can lower into `ContractExecutionIR`, compile back onto the checked callable-bond tree/PDE helper inputs, and execute through `price_dynamic_execution_ir(...)` or `ExecutionBackedPayoff`; the bounded issuer-callable single-index range-accrual cohort can also lower into `ContractExecutionIR` and execute a deterministic call-decision proof over projected checked range-accrual cashflows. Single-underlier autocallable MC/QMC proof work now has `trellis.models.autocallable` as a checked bounded helper for fixed observations, first-trigger redemption, coupon accrual, terminal protection, deterministic discounting, and pseudo/Sobol sampling selection; the dynamic and overlay lanes are still not wired into the authoritative fresh-build pricing compiler | Trellis can now claim route-free executable pricing for the admitted issuer-callable fixed coupon bond proving slice and deterministic issuer-callable single-index range-accrual proof slice, and it can provide a checked bounded helper for agent-written single-underlier autocallable proof adapters, but it is still not support-contract-correct to claim stochastic callable range-accrual valuation, interrupted/barrier-state range-accrual execution, generic dynamic-wrapper execution for autocallables, TARN/TARF, callable coupon structures beyond the checked proof cohorts, swing options, or GMxB-style control or insurance-overlay families | `trellis/agent/dynamic_contract_ir.py`, `trellis/agent/insurance_overlay_contract.py`, `trellis/agent/dynamic_lane_admission.py`, `trellis/agent/semantic_track_classifier.py`, `trellis/agent/knowledge/decompose.py`, `trellis/models/autocallable.py`, `trellis/execution/compiler.py`, `trellis/execution/runtime.py`, `trellis/execution/visitors/event_compile.py`, `trellis/core/payoff.py`, `docs/quant/dynamic_contract_ir.rst`, `docs/user_guide/pricing.rst` |
| L50 | **Bermudan multi-asset rainbow execution is bounded to the P001 proof** — the extension task `P001` now receives a temporally consistent expiry/exercise schedule, emits a route-free execution/operator IR with named `AAPL`/`MSFT` observables, has method-specific capability admission, can execute the Monte Carlo path through correlated GBM plus multi-state Longstaff-Schwartz, can execute the bounded two-underlier lattice path through a generic product-state grid rollback, and the checked-in `_agent` adapter is now only a compatibility shim over the execution visitors. This remains a bounded proof rather than generic multi-asset Bermudan rainbow coverage | Trellis can claim the route-free MC/lattice P001 proof and compatibility-shim closeout, but should not claim full Bermudan rainbow support until broader product generalization is implemented and validated | `TASKS_EXTENSION.yaml`, `trellis/agent/benchmark_contracts.py`, `trellis/agent/invariants.py`, `trellis/execution/compiler.py`, `trellis/execution/admission.py`, `trellis/execution/shims.py`, `trellis/execution/visitors/simulation_bridge.py`, `trellis/models/monte_carlo/lsm.py`, `trellis/models/trees/product_lattice.py`, `trellis/instruments/_agent/rainbowoption.py` |
| L38 | **Binding-first exotic proof coverage is still bounded to the agreed cohort** — the checked proof closeout now passes the full `11`-task benchmark cohort and certifies the honest-block sentinel, but that evidence is still limited to the explicitly measured constructable structures | Trellis can now honestly claim the agreed proof cohort, but it is still not support-contract-correct to claim arbitrary constructable-exotic coverage beyond that bounded benchmark surface | `doc/plan/done__binding-first-exotic-proof-closeout.md`, `docs/benchmarks/binding_first_exotic_proof_closeout.json`, `tests/evals/binding_first_exotic_proof.yaml`, `trellis/agent/backend_bindings.py`, `trellis/agent/family_lowering_ir.py`, `trellis/agent/dsl_lowering.py` |
| L33 | **Book and pipeline abstractions are still thin for mixed exotic books** — the book layer now has bounded reverse-mode lanes for curve-linked bond books, flat/grid-vol European vanilla option books, smooth-interior early-exercise options over flat vol, arithmetic-Asian smooth path summaries, scalar quanto-correlation books, and explicitly configured mixed supported books; the execution layer now has bounded discounted / future-value precursor summaries for the admitted swap workflow, but broader book aggregation still stops well short of a real mixed-exotic portfolio engine and the pipeline scenario surface remains simple | Trellis has stronger factorized book-risk building blocks and a local benchmark gate for the supported AAD lanes, but it is not yet a broad book-level pricing/risk engine even before xVA enters the picture | `trellis/book.py`, `trellis/pipeline.py`, `trellis/analytics/measures.py`, `trellis/analytics/portfolio_aad.py`, `trellis/analytics/benchmarking.py`, `trellis/execution/visitors/aggregation.py`, `scripts/benchmark_portfolio_aad.py` |
| L34 | **Counterparty-risk workflows remain bounded beyond first CVA/DVA/FVA analytics** — Trellis now has a reusable factor-state simulation substrate plus clean future-value cubes for supported vanilla interest-rate swap positions and shared-path swap portfolios, the admitted fixed-float swap execution IR can now feed that same future-value surface route-free, execution-backed precursor visitors can emit deterministic discounted summaries plus bounded exposure-shape outputs, and `trellis.analytics.counterparty` can represent collateral agreements/netting sets, project one netting set's valuation-lagged collateral state over a margin-period horizon, assemble closeout-ready netting-set exposure inputs, compute first `EE`/`EPE`/`PFE` outputs, and price bounded flat-hazard `CVA`/`DVA`/`FVA` under explicit scalar assumptions. Trellis still has no full `MVA/KVA`, stochastic credit, legal enforceability, capital, or funding-desk workflow stack | Counterparty-risk building blocks now include supported swap future-value workflows, first reporting precursors, typed collateral/netting semantics, bounded collateral-state projection, netting-set closeout inputs, reproducible exposure metrics, and first bounded xVA outputs, but enterprise counterparty-risk analytics remain unsupported | `trellis/analytics/counterparty.py`, `trellis/execution/visitors/simulation_bridge.py`, `trellis/execution/visitors/aggregation.py`, `trellis/models/monte_carlo/simulation_substrate.py`, `trellis/book.py`, `doc/plan/done__exotic-desk-roadmap.md` |

### Performance And Scaling

| # | Limitation | Impact | Files |
|---|-----------|--------|-------|
| L35 | **Performance acceleration is local and selective** — there is optional Numba on some kernels, but no multicore, distributed, or GPU execution path | Calibration sweeps, large scenario sets, and portfolio risk runs will hit a scaling ceiling quickly | `trellis/models/_numba.py`, `trellis/models/monte_carlo/engine.py` |
| L36 | **Benchmark coverage is still too narrow** — checked calibration throughput baselines now include desk-like basket-credit and bounded hybrid quanto fixtures with perturbation and latency-envelope metadata alongside the Monte Carlo path-kernel benchmark, pod-risk workflows, the bounded counterparty exposure benchmark pack, and a local bounded portfolio-AAD benchmark gate, but broad exotic-pricing and industrial book-scale risk benchmark surfaces are still missing | Trellis can now compare supported calibration workflows, the first tranche-correlation validation envelope, the bounded hybrid quanto validation envelope, pod-risk workflows, the first institutional exposure reductions, and bounded supported portfolio-AAD lanes over time, but there is still no broad evidence trail for arbitrary exotic pricing throughput or industrial book-scale risk performance | `docs/benchmarks/monte_carlo_path_kernels.json`, `docs/benchmarks/calibration_workflows.json`, `docs/benchmarks/pod_risk_workflows.json`, `docs/benchmarks/counterparty_exposure_workflows.json`, `trellis/analytics/benchmarking.py`, `scripts/benchmark_portfolio_aad.py` |
| L37 | **QMC / variance-reduction tooling is still basic** — Sobol normals, Brownian bridge, antithetics, and a simple control variate exist, but there is no broader simulation-optimization toolkit around them | Useful building blocks exist, but not yet a comprehensive industrial simulation stack | `trellis/models/qmc/__init__.py`, `trellis/models/monte_carlo/variance_reduction.py` |

## Needs Revalidation

These IDs existed in older versions of this file but were not rechecked during
the 2026-04-04 mathematical/numerical/computational pass.

| # | Legacy entry | Prior focus area |
|---|-------------|------------------|
| L7 | Prepayment CPR hard-coded to 6% | mortgage / prepayment |
| L8 | Recovery rate hard-coded to 40% | credit products |
| L13 | Bloomberg provider is a placeholder | data integration |
| L14 | Agent tests require API key | CI / infrastructure |
| L18 | Heston class naming confusion | knowledge system |
| L19 | No `StateSpace` in default test market data | task runner |
| L20 | Agent sometimes omits `MarketState` import | code generation |
| L21 | No copula data contracts | knowledge system |
| L22 | No analytical method requirements | knowledge system |
| L23 | FinancePy imports need writable Numba cache | validation infrastructure |
| L24 | External market-data auto-resolution still discount-curve first | market-data plumbing |
